import "./scheduler.scss";

// Every menu and sometimes items and extras are available
//  at different times on different days. This needs to be configured.
// This scheduler component is a small interface to set available
//  time slots on any day.
// This component will be used in multiple places of the application
//  and should be fully controlled from outside.

export const Scheduler = () => {
  return <div className="scheduler">Scheduler</div>;
};
